#pragma once

#include "timetable/domain/params/make.hpp"

namespace timetable::domain {

    /*
     * Compatibility facade for existing code. New code should include
     * timetable/domain/params/make.hpp when it needs canonical constructors.
     */

}  // namespace timetable::domain
